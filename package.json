{
  "name": "claude-canvas",
  "version": "0.1.0",
  "private": true,
  "description": "AI Agent 畫布工具",
  "scripts": {
    "build:frontend": "cd frontend && bun run build",
    "build:compile": "bun scripts/compile.ts",
    "build:all": "bun run build:frontend && bun run build:compile"
  }
}
